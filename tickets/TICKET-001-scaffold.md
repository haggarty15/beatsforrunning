# TICKET-001: Backend Scaffold

## Description

Set up the core project layout, `.venv`, `pyproject.toml`, `.github/workflows`, `Makefile`, and the initial test framework similar to Dealfinder. Ensure linting and formatting pass properly.

## Acceptance Criteria

- [x] Initial repository structure mapped out
- [x] Tests configuration initialized
- [x] Makefile set up with default target correctly testing project
- [x] GitHub Action set up
